<div class="pb-5">
	<div class="product-item position-relative bg-light d-flex flex-column text-center">
		<img class="img-fluid mb-4" src="{{ asset('img/' ~ product.photo) }}" alt="{{product.name}}">
		<h6 class="text-uppercase">{{product.name}}</h6>
		<h5 class="text-primary mb-0">{{product.price |number_format(2, '.', ',')}}
			€</h5>
		<div class="btn-action d-flex justify-content-center">
			<a class="btn btn-primary py-2 px-3 open-cart-product" data-id="{{product.id}}">
				<i class="bi bi-cart"></i>
			</a>
			<a class="btn btn-primary py-2 px-3 open-info-product" data-id="{{product.id}}">
				<i class="bi bi-eye"></i>
			</a>
		</div>
	</div>
</div>
